<Window x:Class="Lottery_v2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:localViewModel="clr-namespace:Lottery_v2.ViewModel"
        xmlns:tk="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Input.Toolkit"
        xmlns:xctk="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
        mc:Ignorable="d"
        Title="MainWindow" Height="419.698" Width="704.225">
    <Window.Resources>
        <localViewModel:MainWindowProductViewModel x:Key="vmProduct"></localViewModel:MainWindowProductViewModel>
        <localViewModel:MainWindowCustomerViewModel x:Key="vmCustomer"></localViewModel:MainWindowCustomerViewModel>
        <localViewModel:MainWindowSaleViewModel x:Key="vmSale"></localViewModel:MainWindowSaleViewModel>
        <Style TargetType="TabItem">
            <Setter Property="TextElement.Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Background="Transparent" Padding="20,5">
                            <ContentPresenter x:Name="ContentSite" 
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#062543"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True" SourceName="Border">
                                <Setter TargetName="Border" Property="Background" Value="#103458" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate x:Key="autoCustBox">
            <StackPanel Orientation="Horizontal">
                <Label Content="{Binding Name}"/>
                <Label Content="{Binding Agency}" Foreground="DarkGray"/>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="autoProdBox">
            <StackPanel Orientation="Horizontal">
                <Label Content="{Binding Name}"/>
                <Label Content="{Binding Type}" Foreground="DarkGray"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <Grid Background="#084b8a">
        <TabControl TabStripPlacement="Left" SelectedIndex="1">
            <TabItem Header="SATHI AGENCY">
                
            </TabItem>
            <TabItem Header="SALE">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Border Background="Azure" Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Label>CUSTOMER</Label>
                                    <tk:AutoCompleteBox Width="200"
                                                        ItemsSource="{Binding CustomerList, Source={StaticResource vmCustomer}}"
                                                        ValueMemberPath="Name"
                                                        SelectedItem="{Binding SelecSaleCustomer, Source={StaticResource vmCustomer}, Mode=TwoWay}"
                                                        ItemTemplate="{StaticResource autoCustBox}"
                                                        FilterMode="Contains">
                                    </tk:AutoCompleteBox>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Label>ID :</Label>
                                    <Label Content="{Binding SaleCustId, Source={StaticResource vmCustomer}}"/>
                                    <Label>AGENCY:</Label>
                                    <Label Content="{Binding SaleCustAgency,Source={StaticResource vmCustomer}}"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Vertical">
                                <Label>PREVIOUS DUE</Label>
                                <Label Content="{Binding SelecCustDue, Source={StaticResource vmCustomer}}"></Label>
                            </StackPanel>
                            <StackPanel Orientation="Vertical" Grid.Column="2">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Label>DATE :</Label>
                                    <Label></Label>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <!--<Label>BILL ID</Label>
                                <TextBox></TextBox>-->
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <Border Grid.Row="1">
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <Label>PRODUCT</Label>
                                <tk:AutoCompleteBox Width="200"
                                                    ItemsSource="{Binding ProductList, Source={StaticResource vmProduct}}"
                                                    ValueMemberPath="Name"
                                                    SelectedItem="{Binding SelecProduct, Source={StaticResource vmProduct}, Mode=TwoWay}"
                                                    ItemTemplate="{StaticResource autoProdBox}"
                                                    FilterMode="Contains">
                                </tk:AutoCompleteBox>
                                <Label>TYPE : </Label>
                                <Label Content="{Binding SelecType, Source={StaticResource vmProduct}}"></Label>
                                <Label>RATE</Label>
                                <Label Content="{Binding SelecRate, Source={StaticResource vmProduct}}"></Label>
                                <Label>AMOUNT</Label>
                                <Label Content="{Binding SelecAmount, Source={StaticResource vmProduct}}"></Label>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <Label>QUANTITY</Label>
                                <xctk:IntegerUpDown 
                                    Value="{Binding SelecProdQnt, Source={StaticResource vmProduct}}">
                                </xctk:IntegerUpDown>
                                <Slider Width="300" Minimum="0" Maximum="2000" TickFrequency="200" TickPlacement="TopLeft"
                                        Value="{Binding SelecProdQnt, Source={StaticResource vmProduct}}">
                                </Slider>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                        </Grid>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="CUSTOMER">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Border>
                        <Label>CUSTOMER</Label>
                    </Border>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <Border>
                            <ListView
                                ItemsSource="{Binding CustomerList, Source={StaticResource vmCustomer}}"
                                SelectedIndex="{Binding CustomerIndex, Source={StaticResource vmCustomer}}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Border>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="120"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="2*"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>
                                                    <Label Content="{Binding Path=Name}"></Label>
                                                    <Label Grid.Row="1" Content="{Binding Path=Agency}"></Label>
                                                </Grid>
                                                <Grid Grid.Column="1">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="2*"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>
                                                    <Label>Rs.</Label>
                                                    <Label Grid.Row="1" 
                                                        Content="{Binding Path=PreviousDue}">
                                                    </Label>
                                                    <Label Grid.Row="2">DUE</Label>
                                                </Grid>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                        <Border Grid.Column="2">
                            <StackPanel Orientation="Vertical">
                                <Button Command="{Binding AddCustCommand, Source={StaticResource vmCustomer}}">ADD</Button>
                                <Button Command="{Binding EditCustCommand, Source={StaticResource vmCustomer}}">EDIT</Button>
                                <Button>DELETE</Button>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="PRODUCT">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Border>
                            <ListView 
                                ItemsSource="{Binding ProductList, Source={StaticResource vmProduct}}"
                                SelectedIndex="{Binding ProductIndex, Source={StaticResource vmProduct}}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Border>
                                            <StackPanel Orientation="Vertical">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Path=Name}"></TextBlock>
                                                </StackPanel>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Path=Type}"></TextBlock>
                                                </StackPanel>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Path=Rate}"></TextBlock>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                        <StackPanel Orientation="Vertical" Grid.Column="2">
                            <Button Command="{Binding AddBtnClickedCommand, Source={StaticResource vmProduct}}">ADD NEW</Button>
                            <Button Command="{Binding EditBtnClickedCommand, Source={StaticResource vmProduct}}">EDIT</Button>
                            <Button Command="{Binding DeleteBtnClickedCommand, Source={StaticResource vmProduct}}">DELETE</Button>
                        </StackPanel>
                        
                    </Grid>
                    <Border Background="DarkTurquoise" Margin="-3" Padding="10">
                        <Label>PRODUCTS</Label>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
